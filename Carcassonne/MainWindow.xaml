<Window x:Class="Carcassonne.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"        
        xmlns:local="clr-namespace:Carcassonne"
        xmlns:vm="clr-namespace:Carcassonne.ViewModel"
        xmlns:conv="clr-namespace:Carcassonne.Converter"
        xmlns:cal="http://www.caliburnproject.org"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}" Height="800" Width="1200">
    <Window.DataContext>
        <vm:ViewModel x:Name="viewModel"/>
    </Window.DataContext>
    <Window.Resources>
        <conv:Rotation2StringConverter x:Key="rotationConverter"/>
        <conv:CardRotation2IntConverter x:Key="cardRot2Int"/>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="29*"/>
            <RowDefinition Height="497*"/>
            <RowDefinition Height="29*"/>
            <RowDefinition Height="118*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.3*"/>
            <ColumnDefinition Width="10*"/>
            <ColumnDefinition Width="0.3*"/>
        </Grid.ColumnDefinitions>
        <Button Grid.Row="0" Grid.Column="1" Content="U" Command="{Binding MoveBoardUp}"/>
        <Button Grid.Row="1" Grid.Column="0" Content="L" Command="{Binding MoveBoardLeft}"/>
        <Button Grid.Row="1" Grid.Column="2" Content="R" Command="{Binding MoveBoardRight}"/>
        <Button Grid.Row="2" Grid.Column="1" Content="D" Command="{Binding MoveBoardDown}"/>
        <ItemsControl Grid.Row="1" 
                      Grid.Column="1" 
                      ItemsSource="{Binding CardsOnBoard}"
                      AllowDrop="True"
                      Drop ="ItemsControl_Drop"
                      x:Name="boardItemControl"
                      Background="Wheat" Grid.RowSpan="1"
                      >
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemContainerStyle>
                <Style TargetType="ContentPresenter">
                    <Setter Property="Canvas.Left" Value="{Binding GridPosCol}"/>
                    <Setter Property="Canvas.Top" Value="{Binding GridPosRow}"/>
                </Style>
            </ItemsControl.ItemContainerStyle>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Image Width="{Binding Width}" Height="{Binding Height}" Source="{Binding CardImage}">
                        <Image.LayoutTransform>
                            <RotateTransform Angle="{Binding RotationState, Converter={StaticResource cardRot2Int}}"></RotateTransform>
                        </Image.LayoutTransform>
                    </Image>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal" >
            <ListBox ItemsSource="{Binding RotationItems, Mode=TwoWay}" SelectedItem="{Binding CurrentCard.RotationState, Mode=TwoWay}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <!-- <TextBlock Text="{Binding  Converter={StaticResource rotationConverter}}"/> -->
                        <TextBlock Text="{Binding }"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <Button Content="Links" Command="{Binding RotateCurrentCardLeftCommand}"></Button>
            <Image Source="{Binding CurrentCard.CardImage}"
                   MouseMove="Image_MouseMove">
                <Image.LayoutTransform>
                    <RotateTransform Angle="{Binding CurrentCard.RotationState, Converter={StaticResource cardRot2Int}}"/>
                </Image.LayoutTransform>
            </Image>
            <Button Content="Rechts" Command="{Binding RotateCurrentCardRightCommand}"></Button>
            <TextBox Text="{Binding OffsetBoardX}"/>
        </StackPanel>


        <Canvas Grid.Row="1" Grid.Column="1">
            <Ellipse Canvas.Left="{Binding OffsetBoardX}" Canvas.Top="{Binding OffsetBoardY}" Fill="Aqua" Width="50" Height="50"/>
        </Canvas>

    </Grid>
</Window>
